function sortedarray(n1: number[], n2:number[]):number{
    let values:number[] = [];
    
    values.push(...n1);

    values.push(...n2);

    

}

const special = sortedarray([1,3],[2]);
console.log(special);